<!DOCTYPE html>
<html>
  <head>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <% if (result.length != 0) { %>
      <h1>Lista de itens do host(ID) <%= result[0].hostid %> em um grupo especifico %></h1>
      <ul>
        <% result.forEach(function(val) { %>
          <li>Item: <%= val.name %> com ID <%= val.itemid %> e chave <%= val.key_ %>
        <% }) %>
      </ul>
    <% } %>
    <% if (result.length === 0) { %>
      <h1>Nenhum valor foi encontrado</h1>
    <% } %>
  </body>
</html>
